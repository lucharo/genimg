# Render review

Six generated plates were inspected in full. Three initial renders were superseded through ImageGen edits before being embedded:

- Overview: replaced an invented command with Model/Size labels; corrected the unsupported JSON command to `genimg skills list --json`.
- Recipes: removed the implication that a dry-run generated an image. The corrected plate shows a valid plan and a blank planned output frame.
- Contracts: corrected the fabricated skills command, restored the `genimg` prefix and distinguished the grid from carousel state.

The chosen files and hashes are recorded in each plate's manifest. Original renders remain in the task's generated-images directory. Generated pictures explain findings; source excerpts, CLI records and browser captures provide evidence. In the overview, the fox prompt is illustrative text, not a captured GenIMG prompt.
